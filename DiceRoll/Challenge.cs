using System.Diagnostics.CodeAnalysis;

namespace DiceRoll;

public class Challenge
{
    public Challenge() { }

    public int GetSum(List<int> values) {
        // var sum = 0;
        // foreach (var value in values) {
        //     sum += value;
        // }
        // return sum;
        return -1;
    }

    public int GetSumIfThreeMatch(List<int> values) {
        // var dict = new Dictionary<int, int>();
        // var sum = 0;
        // var hasThree = false;
        // foreach (var value in values) {
        //     sum += value;
        //     if (dict.ContainsKey(value)) { dict[value]++; }
        //     else { dict[value] = 1;}
        //     if (dict[value] == 3) hasThree = true;
        // }
        // return hasThree ? sum : 0;
        return -1;
    }
}
